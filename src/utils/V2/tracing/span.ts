// import { Span, SpanContext, trace } from '@opentelemetry/api';
//
// export function createSpan(name: string, context?: SpanContext): Span {
//   const tracer = trace.getTracer('openai-wrapper');
//   return tracer.startSpan(name, undefined, context);
// }
